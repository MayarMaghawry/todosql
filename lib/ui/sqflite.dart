import 'package:flutter/material.dart';

class Sqflite extends StatelessWidget {
  const Sqflite({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('SQLscreen'),centerTitle: true,),
    );
  }
}